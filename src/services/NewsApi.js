export const baseUrl = `https://hacker-news.firebaseio.com/v0/`; // define api baseurl
export const newStoriesUrl = `${baseUrl}newstories.json`; // define newlistId url
export const storyUrl = `${baseUrl}item/`; // define newItem url
